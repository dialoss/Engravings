import React from 'react';

interface test {
    name: string,
    age: number,
    t: object,
}

const petya : test = {
    name: "petya",
    age: 123,
}

const Test = ({data: boolean}) => {
    return (
        <div>

        </div>
    );
};

export default Test;
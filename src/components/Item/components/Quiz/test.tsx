import React from 'react';

interface test {
    name: string,
    age: number,
    t: object,
}


const Test = ({data: boolean}) => {
    return (
        <div>

        </div>
    );
};

export default Test;